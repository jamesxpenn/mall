apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mall-shipping
  name: mall-shipping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mall-shipping
  template:
    metadata:
      labels:
        app: mall-shipping
    spec:
      containers:
      - image: daprxp.azurecr.io/mall-shipping:v1
        name: mall-shipping
        ports:
        - containerPort: 9004
        env:
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
             name: db-mysql
             key: username
        - name: MYSQL_PWD
          valueFrom:
            secretKeyRef:
             name: db-mysql
             key: password             
        - name: MYSQL_HOST
          valueFrom:
            secretKeyRef:
             name: db-mysql
             key: host                 
        - name: MYSQL_PORT
          valueFrom:
            secretKeyRef:
             name: db-mysql
             key: port                 
        - name: NACOS_HOST
          valueFrom:
            secretKeyRef:
             name: nacos-server
             key: host                     
        - name: NACOS_PORT
          valueFrom:
            secretKeyRef:
             name: nacos-server
             key: port
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: mall-shipping
  name: mall-shipping
spec:
  ports:
  - port: 9004
    protocol: TCP
    targetPort: 9004
  selector:
    app: mall-shipping 