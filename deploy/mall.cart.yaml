apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mall-cart
  name: mall-cart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mall-cart
  template:
    metadata:
      labels:
        app: mall-cart
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "mallcart"
        dapr.io/app-port: "9003"         
    spec:
      containers:
      - image: daprxp.azurecr.io/mall-cart:v1
        name: mall-cart
        ports:
        - containerPort: 9003
        env:
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
             name: db-mysql
             key: username
        - name: MYSQL_PWD
          valueFrom:
            secretKeyRef:
             name: db-mysql
             key: password             
        - name: MYSQL_HOST
          valueFrom:
            secretKeyRef:
             name: db-mysql
             key: host                 
        - name: MYSQL_PORT
          valueFrom:
            secretKeyRef:
             name: db-mysql
             key: port                 
        - name: NACOS_HOST
          valueFrom:
            secretKeyRef:
             name: nacos-server
             key: host                     
        - name: NACOS_PORT
          valueFrom:
            secretKeyRef:
             name: nacos-server
             key: port
        - name: REDIS_HOST
          valueFrom:
            secretKeyRef:
             name: db-redis
             key: host
        - name: REDIS_PORT
          valueFrom:
            secretKeyRef:
             name: db-redis
             key: port
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
             name: db-redis
             key: password
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: mall-cart
  name: mall-cart
spec:
  ports:
  - port: 9003
    protocol: TCP
    targetPort: 9003
  selector:
    app: mall-cart